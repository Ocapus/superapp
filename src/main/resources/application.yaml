spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/superapp
    username: superapp
    password: supersecret
  jpa:
    hibernate:
      ddl-auto: validate   # Flyway gestionará el esquema
    properties:
      hibernate:
        format_sql: true
        show_sql: false
flyway:
  baseline-on-migrate: true